let myTime = 0.0;
let startTime = new Date();

function advanceTime() {
    myTime += 1.0;
    elapsedTime = ((new Date() - startTime)/1000.0).toFixed(1);
    console.log(`myTime = ${myTime}, elapsedTime = ${elapsedTime}`);
}
// What will this do?
setTimeout(advanceTime, 1000);
setTimeout(advanceTime, 1000);
setTimeout(advanceTime, 1000);
setTimeout(advanceTime, 1000);
